const router = require("express").Router();
const controller = require("../controller/testController");

// router.get("/testEndpoint", controller.testEnd);
// router.post("")

module.exports = router;
